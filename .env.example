# Copy to .env.local. Without these the site still runs; form submissions are
# logged to the server console instead of emailed.
RESEND_API_KEY=
INQUIRY_TO=        # employer leads inbox, defaults to info@vertisglobal.com
CANDIDATE_TO=      # resume inbox, defaults to INQUIRY_TO
NEWSLETTER_TO=     # newsletter signups, defaults to INQUIRY_TO
